{
  "name": "bun-db-mcp",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start-stdio": "bun run src/index.ts --transport stdio",
    "start-sse": "bun run src/index.ts --transport sse",
    "start-http": "bun run src/index.ts --transport http --oauth",
    "debug-http": "ts-node-esm src/index.ts --transport http --oauth",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:db": "bun test tests/db.test.ts",
    "test:tools": "bun test tests/tools.test.ts",
    "test:watch": "bun test --watch",
    "build": "bun build --outfile=dist/run.js --target=node ./src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.2.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "google-auth-library": "^10.3.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.3"
  }
}